import styles from '../styles/Components/MedicineTable.module.css'

export default function MedicineTable() {
    const username = '';
    const email = '';
    const id = '';
    
    return (
        <div>
            <h1>Welcome to page {username}</h1>
            <h1>Your Email is: {email}</h1>
            <h1>id: {id}</h1>
        </div>
    )
}